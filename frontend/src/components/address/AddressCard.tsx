import type { Address } from '@/lib/services/addressService';
export default function AddressCard({address,onEdit,onDelete,onDefault,select}:{address:Address;onEdit?:()=>void;onDelete?:()=>void;onDefault?:()=>void;select?:()=>void}) { return <article className="border border-[#ddd] p-5"><div className="mb-3 flex items-center gap-2"><h3 className="font-bold">{address.addressName}</h3>{address.isDefault&&<span className="bg-[#222] px-2 py-0.5 text-xs text-white">기본 배송지</span>}</div><p className="text-sm font-medium">{address.recipientName} · {address.phone}</p><p className="mt-2 text-sm text-[#666]">[{address.postalCode}] {address.roadAddress} {address.detailAddress} {address.extraAddress}</p>{address.deliveryRequest&&<p className="mt-2 text-xs text-[#888]">요청: {address.deliveryRequest}</p>}<div className="mt-4 flex flex-wrap gap-2">{select&&<button onClick={select} className="bg-[#222] px-3 py-1.5 text-xs text-white">선택</button>}{!address.isDefault&&onDefault&&<button onClick={onDefault} className="border px-3 py-1.5 text-xs">기본 배송지 설정</button>}{onEdit&&<button onClick={onEdit} className="border px-3 py-1.5 text-xs">수정</button>}{onDelete&&<button onClick={onDelete} className="border border-red-300 px-3 py-1.5 text-xs text-red-600">삭제</button>}</div></article>; }
