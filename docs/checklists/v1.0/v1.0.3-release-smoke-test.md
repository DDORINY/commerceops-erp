# v1.0.3 릴리스 Smoke Test 체크리스트

## 작업 목적

최종 포트폴리오 릴리스 전 backend test, frontend lint/build, 문서 공백 검증을 수행하고 smoke test 결과를 문서화한다.

## 완료 조건

- [x] main 최신 상태 확인
- [x] `v1.0.3-release-smoke-test` 브랜치 생성
- [x] backend test 실행
- [x] frontend lint 실행
- [x] frontend build 실행
- [x] 기본 `.next` EPERM 발생 시 별도 dist로 재검증
- [x] 임시 build output 삭제
- [x] `tsconfig.json` 임시 include 제거
- [x] smoke test 결과 문서 생성
- [x] `git diff --check`
- [x] main 병합
- [x] `v1.0.3` 태그 생성 및 push

## 변경 파일

- `docs/operations/RELEASE_SMOKE_TEST.md`
- `docs/checklists/v1.0/v1.0.3-release-smoke-test.md`

## 검증 결과

- backend test: 통과
- frontend lint: 통과
- frontend build: 기본 `.next`는 Windows `spawn EPERM`으로 실패
- frontend build 재검증: `NEXT_DIST_DIR=.next-build-check-v103`로 통과
- 임시 build output: 삭제 완료
- `tsconfig.json`: 임시 include 제거 완료
- `git diff --check`: 통과

## 남은 이슈

- 브라우저 수동 smoke test는 `docs/operations/RELEASE_SMOKE_TEST.md`의 후보 목록 기준으로 최종 제출 전 확인한다.
