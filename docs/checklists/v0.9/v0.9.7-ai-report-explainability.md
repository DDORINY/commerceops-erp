# v0.9.7 AI 리포트/근거 설명 체크리스트

## 목적

v0.9 AI 운영 화면의 점수와 근거를 운영자가 해석할 수 있도록 AI 리포트 카드와 근거 설명 API/UI를 추가한다.

## 작업 범위

- [x] main 최신 상태 확인
- [x] `v0.9.7-ai-report-explainability` 브랜치 생성
- [x] `AiReportResponse` DTO 추가
- [x] AI 리포트 service 추가
- [x] `GET /api/admin/ai/reports` API 추가
- [x] `AI_REPORT_READ` 권한 적용
- [x] `/admin/ai/reports` 관리자 화면 추가
- [x] 관리자 사이드바 AI 리포트 메뉴 추가
- [x] 관련 문서 갱신
- [x] backend test 실행
- [x] frontend lint 실행
- [x] frontend build 실행
- [x] `git diff --check` 실행
- [x] main 병합
- [x] `v0.9.7` 태그 생성 및 push

## API 엔드포인트

| Method | Path | 권한 | 설명 |
| --- | --- | --- | --- |
| `GET` | `/api/admin/ai/reports` | `AI_REPORT_READ` | AI 운영 모듈별 리포트와 해석 기준을 조회한다. |

## 검증 결과

- backend test: 통과 (`.\gradlew.bat test --no-daemon`)
- frontend lint: 통과 (`npm.cmd run lint`)
- frontend build: 통과 (`NEXT_DIST_DIR=.next-build-check-v097 npm.cmd run build`)
- `git diff --check`: 통과
- 참고: 일반 build가 Windows `spawn EPERM`으로 실패해 권한 상승으로 재실행했다. Next가 추가한 `tsconfig.json` 임시 include는 제거했고 `.next-build-check-v097` 산출물도 삭제했다.

## 남은 이슈

- SHAP/LIME 같은 고급 설명 모델, 외부 LLM 요약, 리포트 파일 다운로드는 제외한다.
- v0.9.8에서 포트폴리오 데모 가이드를 정리한다.
