# v0.1.1 관리자 문의 관리 실제 API 연결

## 기본 정보

- 버전: `v0.1.1`
- 브랜치: `v0.1.1-admin-inquiries-api`
- 작업명: 관리자 문의 관리 실제 API 연결 정리
- 작업 목적: 관리자 문의 관리 화면이 실제 API 기반으로 목록 조회, 답변, 종료 처리를 안정적으로 수행하도록 정리한다.
- 시작일: 2026-07-04
- 완료일: 2026-07-04

## 범위

- [x] 이번 버전에서 할 일: 관리자 문의 목록/검색/상태 필터/답변/종료 흐름 점검 및 보완
- [x] 이번 버전에서 하지 않을 일: 신규 알림 기능, 이메일 발송, 대규모 문의 도메인 리팩터링
- [x] 영향을 받는 화면/API/DB: 관리자 문의 화면, `/api/admin/inquiries`, 문의 엔티티 조회

## 구현 체크리스트

- [x] 작업 전 현재 `main` 최신화
- [x] 버전 규칙에 맞는 브랜치 생성
- [x] 기존 API/서비스/화면 구조 확인
- [x] mock 데이터 위치 확인
- [x] 필요한 코드 구현
- [x] 로딩 상태 처리
- [x] 에러 상태 처리
- [x] 데이터 없음 상태 처리
- [x] 권한/JWT 처리 확인
- [x] TypeScript 타입 또는 백엔드 DTO 정리
- [x] 불필요한 mock 데이터 제거
- [x] 관련 문서 갱신

## 검증 체크리스트

- [x] frontend `npm.cmd run lint`
- [x] frontend `npm.cmd run build`
- [x] backend `.\gradlew.bat test`
- [x] 수동 확인 또는 주요 화면 확인
- [x] 임시 빌드 산출물 정리
- [x] `tsconfig.json` 임시 include 원복 확인

## 결과

- 변경 파일: `frontend/src/app/admin/inquiries/page.tsx`, `frontend/src/lib/services/inquiryService.ts`, `backend/src/main/java/com/commerceops/erp/global/security/SecurityConfig.java`, `docs/checklists/v0.1.1-admin-inquiries-api.md`
- API 엔드포인트: `GET /api/admin/inquiries`, `PATCH /api/admin/inquiries/{id}/answer`, `PATCH /api/admin/inquiries/{id}/close`
- mock 제거 여부: 관리자 문의 화면 내 mock 데이터 사용 없음 확인
- 검증 결과: frontend lint 통과, frontend build 별도 `NEXT_DIST_DIR=.next-build-check-v011`로 통과, backend test 통과
- 남은 이슈: 기본 `.next` 빌드는 기존 잠금으로 EPERM 발생하여 별도 dist로 검증

## 릴리스

- [x] 커밋 완료
- [x] 원격 브랜치 push 완료
- [x] `main` 병합 완료
- [x] 태그 생성 완료
- [x] 태그 push 완료
