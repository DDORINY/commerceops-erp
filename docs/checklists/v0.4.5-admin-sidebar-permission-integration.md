# v0.4.5 Admin Sidebar Permission Integration Checklist

## 목적

관리자 사이드바와 관리자 layout guard를 `admin_menu_permissions`와 현재 사용자 effective permission 기준으로 연동한다.

## 브랜치

- 작업 브랜치: `v0.4.5-admin-sidebar-permission-integration`

## 구현 범위

- [x] main 최신 상태 확인
- [x] v0.4.4 권한 매트릭스 구조 확인
- [x] `Permission`, `PermissionGroup`, `UserPermissionGroup`, `AdminMenuPermission` 구조 확인
- [x] `PermissionMatrixService` effective permission 계산 흐름 확인
- [x] `GET /api/admin/users/me/permissions` 추가
- [x] `GET /api/admin/menu-permissions`를 `MANAGER`도 조회 가능하게 보강
- [x] 사이드바 menuKey와 DB `admin_menu_permissions.menu_key` 매칭 기준 정리
- [x] `V13__seed_admin_sidebar_menu_permissions.sql` 추가
- [x] `frontend/src/lib/adminMenu.ts` 추가
- [x] `AdminSidebarV2`를 permission code 기반 메뉴 노출로 전환
- [x] `AdminLayout`에 직접 URL 접근 권한 확인과 403 안내 UX 추가
- [x] 권한 API 실패 시 role fallback 유지
- [x] query string active 판정 유지
- [x] 단일 open group 아코디언 UX 유지

## menuKey 매칭

- [x] `dashboard`, `products`, `categories`, `banners`, `orders`, `shipments`, `returns`, `inquiries`, `reviews`, `inventory`, `warehouses`, `sales`, `accounting`, `coupons`, `staff`, `permission-groups`, `roles`, `menu-permissions`, `audit-logs`, `settings`는 기존 v0.4.4 seed와 매칭
- [x] `product-create`, `product-status`, `top-navigation`, `refunds`, `inbound`, `stock-transfers`, `payments`, `refund-history`, `settlements`, `settings-company`, `settings-terms` 등 query/detail 메뉴는 v0.4.5 seed로 보강

## 권한 정책

- [x] `SUPER_ADMIN`: visible 메뉴 전체 노출
- [x] `ADMIN`/`MANAGER`: effective permission code와 메뉴별 required permission 기준 노출
- [x] 권한 조회 성공 시 permission 기준 우선
- [x] 권한 조회 실패 시 기존 role 기반 fallback 적용
- [x] API method 단위 permission 세분화는 v0.4.6으로 이관

## 문서 변경

- [x] `docs/API.md` 갱신
- [x] `docs/DB_SCHEMA.md` 갱신
- [x] `docs/BACKEND_STRUCTURE.md` 갱신
- [x] `docs/FRONTEND_STRUCTURE.md` 갱신
- [x] `docs/CURRENT_STATE.md` 갱신
- [x] `docs/VERSION_PLAN.md` 갱신
- [x] `docs/V0.4_HR_PERMISSION_PLAN.md` 갱신

## 검증 결과

- [x] frontend `npm.cmd run lint` 통과
- [x] frontend `npm.cmd run build` 기본 `.next` EPERM으로 실패
- [x] frontend `NEXT_DIST_DIR=.next-build-check-v045 npm.cmd run build` 일반 실행 EPERM 후 권한 상승 재실행으로 통과
- [x] `.next-build-check-v045` 임시 산출물 삭제 완료
- [x] Next가 추가한 `tsconfig.json` 임시 include 제거 완료
- [x] backend `./gradlew.bat test` 통과
- [x] 기본 Gradle 실행은 wrapper lock 접근 거부로 실패했고, 권한 상승 재실행으로 통과

## 남은 이슈

- [x] API permission 기반 세분화는 v0.4.6으로 이관
- [x] 권한 변경 승인 워크플로우는 제외
- [x] 조직도 기반 권한 상속은 제외
- [x] 수동 브라우저 권한 계정별 확인은 후속 QA 항목으로 유지

## 완료 조건

- [x] AdminSidebarV2 permission code 기반 메뉴 노출
- [x] admin_menu_permissions와 menuKey 매칭
- [x] 사용자 effective permissions 기준 메뉴 필터링
- [x] role fallback 정책 구현
- [x] 비인가 직접 접근 UX 구현
- [x] 관련 문서와 체크리스트 갱신
- [x] frontend lint/build 통과
- [x] backend test 통과
- [x] 커밋 생성
- [x] 원격 브랜치 push
- [x] main 병합
- [x] `v0.4.5` 태그 생성 및 push
